version: "3.9"
services:
  php-apache:
    ports:
      - "80:80"
    build: './build/php'
    volumes:
      - .:/var/www/html/
      - ./website/apache/:/etc/apache2/sites-available/
  mysql:
    ports:
      - "3306:3306"
    build: './build/mysql'
    environment:
      MYSQL_ROOT_PASSWORD: "password"
      MYSQL_USER: "user"
      MYSQL_PASSWORD: "password"
      MYSQL_DATABASE: "BIKE_SHOP"
    volumes:
      - dbData:/var/lib/mysql
      - ./build/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql  # Add this line
volumes:
  app:
  dbData:
